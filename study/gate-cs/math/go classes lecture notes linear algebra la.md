when we have an incosistenet equatio of the form <0000...> = 1 or something then we cant have same number of columns as same number of linearly independent columns. 
Ie not all columns can be indpeendant.

But since the Ax = B one row is whole zero but the correpsonding one is not zero. Hence the left hand side thing is of lower dimension and B is not zero so in this case there cant be any solution what so ever.

----

LINEAR dependece doesnt require parition of all vectors (their coefficeint is zero) ...it transletes to the fact that some varaibles can take value zero as a solution to an equation AS = B.
if there is a soltuion X with some rows as zero, we still take X as solution and we say that B is linear combination of A, even tough some columns of A have no praitipicaitaion.


Linear depednace is about ability to create a given vectors from a set of vectors. Talking about linear dependace amoung a set is stupid, of course.

----

The most important thing:
If A(mn), m >n then the equations must be consitentet ie reaudce the colums of AX = B augmented martix to zero....
If m<n then if there is are m independct columns then soltuion exist (infact infinite).
if there are less then m LI columns then solution may or may not exist (not enough vectors....required for B with m rows).

if m=n and m LI then unique solution
if <m LI then not enough vectors hece solution may or may not exist
With B = 0  a solution alwyas exsits (all zero solution).. okays


-------

there are two geometric views to soling eqautions. one is vector. one is line, plane ettc....
ok

and two ways to do multiplcation of matrixes: one is dot product (ugly) and one is linear combinatiaon view (good)