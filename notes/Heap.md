Min and max heap
In min heap max element is always pereset in leaf
but max may also be present as internal node (duplicate stuff).
heaps support duplicates naturally.
if max is internal node then all of its child will be same duplicate. same same buppy same.


[Heap sort](../algo/obsidian-notes/Heap%20sort.md)
